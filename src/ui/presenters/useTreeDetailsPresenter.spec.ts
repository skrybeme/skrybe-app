describe(`useTreeDetailsPresenter`, () => {
  describe(`accessing tree details data`, () => {
    it.todo(`returns loading state on mount`);

    it.todo(`eventually returns tree view model delivered by the use case`);
  });

  describe(`generateChildrenTreeNodes`, () => {
    it.todo(`executes an use case`);

    it.todo(`changes tree details loading state`);

    it.todo(`eventually updates tree details data`);
  });

  describe(`insertTreeNode`, () => {
    it.todo(`executes an use case`);

    it.todo(`changes tree details loading state`);

    it.todo(`eventually updates tree details data`);
  });

  describe(`removeTreeNode`, () => {
    it.todo(`executes an use case`);

    it.todo(`changes tree details loading state`);

    it.todo(`eventually updates tree details data`);
  });

  describe(`updateTreeNode`, () => {
    it.todo(`executes an use case`);

    it.todo(`changes tree details loading state`);

    it.todo(`eventually updates tree details data`);
  });
});
