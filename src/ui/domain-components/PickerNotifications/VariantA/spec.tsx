describe(`Domain component: PickerNotifications`, () => {
  describe(`insistent early subscribers prompt`, () => {});

  describe(`non-insistent early subscribers prompt`, () => {});

  it.todo(`renders without email input by default`);

  describe(`when 'Keep me posted' button is clicked`, () => {
    it.todo(`renders with email input`);

    it.todo(`renders with form submit/cancel buttons`);

    it.todo(`hides 'Keep me posted' button`);
  });
});
