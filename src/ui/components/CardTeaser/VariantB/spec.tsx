import { describeCardTeaser } from '../describeCardTeaser';
import { CardTeaser_VariantB } from '.';

describe(`Components: CardTeaser_VariantB`, () => {
  describeCardTeaser(CardTeaser_VariantB);
});
