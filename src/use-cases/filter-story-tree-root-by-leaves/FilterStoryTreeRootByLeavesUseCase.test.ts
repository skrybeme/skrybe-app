describe(`FilterStoryTreeRootByLeavesUseCase`, () => {
  describe(`when the argument is falsy`, () => {
    it.todo(`does not change filtered collection`);
  });

  describe(`when the argument is truthy`, () => {
    it.todo(`does not include non-leave story tree nodes in filtered collection`);

    it.todo(`includes all leave story tree nodes in filtered collection`);
  });
});
