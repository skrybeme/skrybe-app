export const flex = () => `
  display: flex;
  align-items: center;
  justify-content: center;
`;

export const noSelect = () => `
  -webkit-touch-callout: none;
  user-select: none;
`;

export const positionCover = () => `
  top: 0;
  left: 0;
  bottom: 0;
  right: 0;
  position: absolute;
`;
